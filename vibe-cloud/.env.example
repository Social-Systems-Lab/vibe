# Vibe Cloud Environment Variables

# --- General ---
# The port the Vibe Cloud server will listen on
APP_PORT=3000

# --- Security ---
# A strong, random secret for signing JWT tokens
# Generate using: openssl rand -base64 32
JWT_SECRET=

# --- CouchDB ---
# Connection details for the CouchDB instance
COUCHDB_URL=http://couchdb:5984
COUCHDB_USER=admin
COUCHDB_PASSWORD=password # Change this in production!

# --- Minio (S3) ---
# Connection details for the Minio instance
MINIO_ENDPOINT=minio
MINIO_PORT=9000
MINIO_USE_SSL=false # Set to true if using HTTPS
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin # Change this in production!
MINIO_BUCKET_NAME=vibe-blobs
