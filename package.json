{
    "name": "vibe-monorepo",
    "version": "0.0.1",
    "private": true,
    "description": "Vibe MVP Monorepo",
    "scripts": {
        "build": "turbo build",
        "dev": "turbo dev --parallel",
        "lint": "turbo lint",
        "test": "turbo test",
        "clean": "turbo clean && rm -rf node_modules"
    },
    "devDependencies": {
        "turbo": "latest",
        "typescript": "^5.0.0"
    },
    "workspaces": [
        "apps/*",
        "packages/*"
    ]
}
