{
    "private": true,
    "name": "vibe",
    "workspaces": [
        "apps/*",
        "packages/*",
        "infra"
    ],
    "scripts": {
        "build": "pnpm -r build",
        "dev": "pnpm -r --parallel dev"
    },
    "dependencies": {
        "undici": "^7.10.0"
    },
    "devDependencies": {
        "concurrently": "^9.2.0",
        "@types/varint": "^6.0.0"
    }
}
